
  Morty Game 
  
         <div>
            <Stage>
    <Sprite image={Morty} x={100} y={100} />
  </Stage>
            <Stage>
            {morties.map((item, index) => 
            <Sprite image={Morty} x={100} y={100} onClick={()=>multiMorty(index)}/>)}    
    
  </Stage>
            {morties.map((item, index) => <p>
                <img src={Morty} style={item.class} onClick={()=>multiMorty(index)}></img></p>)}
            </div>

